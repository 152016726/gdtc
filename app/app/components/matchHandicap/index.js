/**
 * Created by Roger(ljx) on 2018/10/16.
 */
import React,{Component} from 'react';
import {View,Text, Image, StyleSheet, TouchableOpacity, Animated, FlatList} from 'react-native';
import * as CommonColor from '../../constants/color';

export default class MatchHandicap extends Component{
    // 构造
      constructor(props) {
        super(props);
        // 初始状态
        this.state = {};
      }

    render(){
        return (
            <View>

            </View>
        )
    }
}

const styles = StyleSheet.create({

});
/**
 * 定义该页面路由
 * liveWords 文字直播
 * realtimeMatch 实时赛况
 * teamLineup 球队阵容
 * @type {string[]}
 */
const pages = ["liveWords", "eventSituation", "teamLineup"]; //, "realtimeMatch", "teamLineup"

module.exports = pages;